/**
 * @typedef {Object} APIResponseBreeds Server response with breeds data
 * @prop {Object<string, string[]>} message The object with the list of breeds
 * @prop {string} status Request status
 */

/**
 * @typedef {Object} APIResponseBreedsImages Server response with URLs of breeds images
 * @prop {string[]} message The array with the URLs of breeds images
 * @prop {string} status Request status
 */
